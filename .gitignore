runs/
# .github/hooks/ is GENERATED by install-hooks.sh (scripts synced from
# single-branch-development/scripts/, wiring from templates/). Not committed in this
# catalog repo — run the installer to materialize it. A repo that USES these skills
# should instead COMMIT its .github/hooks/ so the bundle travels into every worktree.
.github/hooks/
